import { Box, Typography } from "@mui/material";

const Inicio = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "31.8rem",
      }}
    >
      <Typography>Componente principal</Typography>
    </Box>
  );
};

export default Inicio;
